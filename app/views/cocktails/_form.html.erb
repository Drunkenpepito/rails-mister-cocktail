 <div class="container">
   <%= simple_form_for cocktail do |f| %>
    <%= f.input :name,label: 'What is the name of your cocktail?'%>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Submit ", class: "btn btn-primary" %>
  <% end %>
</div>
