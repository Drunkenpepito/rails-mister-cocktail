 <%= simple_form_for [@cocktail, dose] do |f| %>
  <%= f.input :description, label: 'Special Name' %>
  <%= f.association :ingredient %>
  <%= f.input :amount %>
  <%= f.input :unit %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
