 <%= simple_form_for [@cocktail, dose] do |f| %>
  <%= f.input :description, label: 'Comment faire?' %>
  <%= f.association :ingredient %>
<!--   <%= f.input :amount %>
  <%= f.input :unit %> -->
  <%= f.submit "Ajouter", class: "btn btn-primary" %>
<% end %>
